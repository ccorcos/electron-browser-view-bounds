# Electron BrowserView resize bug

This repro shows that when you resize a the window with a browser view in there, something doesn't work corretly with the click events.

[example.mov](./example.mov)